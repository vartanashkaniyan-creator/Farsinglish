<!DOCTYPE html>
<html lang="fa">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Farsinglish - تست UI کامپوننت‌ها (بخش ۲)</title>
    <style>
        * { margin: 0; padding: 0; box-sizing: border-box; }
        body { 
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'Roboto', sans-serif;
            background: linear-gradient(135deg, #1e2b3a 0%, #2c3e50 100%);
            color: #ecf0f1;
            padding: 20px;
        }
        .container { 
            max-width: 900px; 
            margin: 0 auto;
            background: rgba(52, 73, 94, 0.95);
            border-radius: 20px;
            padding: 25px;
            box-shadow: 0 15px 40px rgba(0,0,0,0.6);
        }
        h1 { text-align: center; color: #ecf0f1; border-bottom: 3px solid #e67e22; padding-bottom: 15px; }
        
        /* مودال */
        .modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0,0,0,0.8);
            justify-content: center;
            align-items: center;
            z-index: 1000;
        }
        .modal-content {
            background: #34495e;
            padding: 30px;
            border-radius: 20px;
            max-width: 500px;
            width: 90%;
            animation: modalSlide 0.3s;
            border: 2px solid #e67e22;
        }
        @keyframes modalSlide {
            from { transform: translateY(-50px); opacity: 0; }
            to { transform: translateY(0); opacity: 1; }
        }
        
        /* فرم‌ها */
        .form-group {
            margin: 15px 0;
        }
        .form-input {
            width: 100%;
            padding: 12px;
            border-radius: 8px;
            border: 2px solid #7f8c8d;
            background: #2c3e50;
            color: white;
            font-size: 1rem;
        }
        .form-input:focus {
            border-color: #e67e22;
            outline: none;
        }
        
        /* کارت SRS */
        .srs-card {
            background: rgba(230, 126, 34, 0.1);
            border: 1px solid #e67e22;
            border-radius: 15px;
            padding: 20px;
            margin: 15px 0;
        }
        .progress-ring {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: conic-gradient(#e67e22 0deg 144deg, #7f8c8d 144deg 360deg);
            display: flex;
            justify-content: center;
            align-items: center;
        }
        .progress-inner {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: #34495e;
            display: flex;
            justify-content: center;
            align-items: center;
            font-weight: bold;
        }
        
        /* فلش کارت */
        .flashcard {
            background: #2c3e50;
            border-radius: 15px;
            padding: 30px;
            text-align: center;
            cursor: pointer;
            transition: transform 0.6s;
            transform-style: preserve-3d;
            position: relative;
            margin: 20px 0;
            border: 2px solid #f39c12;
        }
        .flashcard.flipped {
            transform: rotateY(180deg);
        }
        .flashcard-front, .flashcard-back {
            backface-visibility: hidden;
        }
        .flashcard-back {
            transform: rotateY(180deg);
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            background: #e67e22;
            border-radius: 15px;
        }
        
        .badge {
            display: inline-block;
            padding: 5px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: bold;
        }
        .badge.easy { background: #27ae60; }
        .badge.medium { background: #f39c12; }
        .badge.hard { background: #e74c3c; }
        
        .console {
            background: #1e2b3a;
            padding: 15px;
            border-radius: 10px;
            font-family: monospace;
            color: #ecf0f1;
            max-height: 200px;
            overflow-y: auto;
            margin-top: 20px;
            border: 1px solid #e67e22;
        }
        
        .btn {
            padding: 12px 24px;
            border-radius: 30px;
            border: none;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: 0.3s;
            margin: 5px;
        }
        .btn-orange { background: #e67e22; color: white; }
        .btn-purple { background: #9b59b6; color: white; }
        .btn:hover { transform: translateY(-2px); box-shadow: 0 5px 15px rgba(230,126,34,0.4); }
    </style>
</head>
<body>
    <div class="container">
        <h1>🧪 تست UI کامپوننت‌ها - بخش ۲ (پیشرفته)</h1>
        
        <!-- مودال -->
        <div class="component-card" style="background: rgba(52,73,94,0.8); padding:20px; border-radius:15px; margin:20px 0;">
            <h3>🪟 مودال</h3>
            <button class="btn btn-orange" onclick="UITest2.openModal('success')">مودال موفقیت</button>
            <button class="btn btn-orange" onclick="UITest2.openModal('error')">مودال خطا</button>
            <button class="btn btn-orange" onclick="UITest2.openModal('srs')">مودال SRS</button>
        </div>
        
        <!-- فرم ثبت‌نام -->
        <div class="component-card" style="background: rgba(52,73,94,0.8); padding:20px; border-radius:15px; margin:20px 0;">
            <h3>📝 فرم ثبت‌نام</h3>
            <div class="form-group">
                <input type="text" class="form-input" id="username" placeholder="نام کاربری">
            </div>
            <div class="form-group">
                <input type="email" class="form-input" id="email" placeholder="ایمیل">
            </div>
            <div class="form-group">
                <input type="password" class="form-input" id="password" placeholder="رمز عبور">
            </div>
            <button class="btn btn-orange" onclick="UITest2.validateForm()">ثبت‌نام</button>
        </div>
        
        <!-- کارت SRS و مرور هوشمند -->
        <div class="component-card" style="background: rgba(52,73,94,0.8); padding:20px; border-radius:15px; margin:20px 0;">
            <h3>🧠 کارت SRS</h3>
            <div class="srs-card">
                <div style="display: flex; justify-content: space-between; align-items: center;">
                    <div>
                        <h4 style="color: #e67e22;">cat</h4>
                        <span class="badge easy">سطح آسان</span>
                    </div>
                    <div class="progress-ring">
                        <div class="progress-inner">۴۰٪</div>
                    </div>
                </div>
                <div style="margin-top: 15px;">
                    <div style="display: flex; justify-content: space-between; font-size:0.9rem;">
                        <span>📅 مرور بعدی: فردا</span>
                        <span>🔄 فاصله: ۶ روز</span>
                    </div>
                    <div style="display: flex; gap: 10px; margin-top: 15px;">
                        <button class="btn btn-success" style="flex:1;" onclick="UITest2.logSRS(5)">🔥 عالی (۵)</button>
                        <button class="btn btn-primary" style="flex:1;" onclick="UITest2.logSRS(3)">👍 خوب (۳)</button>
                        <button class="btn btn-warning" style="flex:1;" onclick="UITest2.logSRS(1)">🔄 دوباره (۱)</button>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- فلش کارت -->
        <div class="component-card" style="background: rgba(52,73,94,0.8); padding:20px; border-radius:15px; margin:20px 0;">
            <h3>🃏 فلش کارت</h3>
            <div class="flashcard" id="flashcard" onclick="UITest2.flipCard()">
                <div class="flashcard-front">
                    <h2 style="color: #f39c12;">cat</h2>
                    <p style="color: #bdc3c7;">/kæt/</p>
                </div>
                <div class="flashcard-back">
                    <h2 style="color: white;">گربه</h2>
                </div>
            </div>
            <div style="display: flex; justify-content: center; gap: 15px; margin-top: 10px;">
                <button class="btn btn-success" onclick="UITest2.flipResult(true)">✅ بلدم</button>
                <button class="btn btn-warning" onclick="UITest2.flipResult(false)">❌ بلد نیستم</button>
            </div>
        </div>
        
        <!-- تایمر و پیشرفت -->
        <div class="component-card" style="background: rgba(52,73,94,0.8); padding:20px; border-radius:15px; margin:20px 0;">
            <h3>⏱️ تایمر مطالعه</h3>
            <div style="display: flex; align-items: center; gap: 20px;">
                <div style="background: #2c3e50; padding: 20px; border-radius: 50%; width: 80px; height: 80px; display: flex; justify-content: center; align-items: center;">
                    <span id="timer" style="font-size: 1.8rem; font-weight: bold; color: #e67e22;">۰۵:۰۰</span>
                </div>
                <div>
                    <button class="btn btn-success" onclick="UITest2.startTimer()">▶️ شروع</button>
                    <button class="btn btn-warning" onclick="UITest2.pauseTimer()">⏸️ توقف</button>
                    <button class="btn btn-danger" onclick="UITest2.resetTimer()">🔄 ریست</button>
                </div>
            </div>
        </div>
        
        <!-- کنسول لاگ -->
        <div class="console" id="console">
            [سیستم] تست کامپوننت‌های پیشرفته - بخش ۲ آماده است
        </div>
    </div>
    
    <!-- مودال -->
    <div id="modal" class="modal">
        <div class="modal-content" id="modalContent">
            <!-- محتوای مودال با JS پر می‌شود -->
        </div>
    </div>

    <script>
        const UITest2 = {
            log(message, type = 'info') {
                const console = document.getElementById('console');
                const timestamp = new Date().toLocaleTimeString('fa-IR');
                let color = '#ecf0f1';
                if (type === 'success') color = '#2ecc71';
                if (type === 'error') color = '#e74c3c';
                if (type === 'warning') color = '#f39c12';
                
                console.innerHTML += `<br><span style="color: ${color};">[${timestamp}] ${message}</span>`;
                console.scrollTop = console.scrollHeight;
            },
            
            // مودال
            openModal(type) {
                const modal = document.getElementById('modal');
                const content = document.getElementById('modalContent');
                
                let html = '';
                if (type === 'success') {
                    html = `
                        <h3 style="color: #2ecc71; margin-bottom: 15px;">✅ عملیات موفق</h3>
                        <p>درس با موفقیت ذخیره شد.</p>
                        <button class="btn btn-success" onclick="UITest2.closeModal()" style="margin-top: 20px;">باشه</button>
                    `;
                    this.log('🪟 مودال موفقیت باز شد', 'success');
                } else if (type === 'error') {
                    html = `
                        <h3 style="color: #e74c3c; margin-bottom: 15px;">❌ خطا</h3>
                        <p>اتصال به سرور برقرار نشد.</p>
                        <button class="btn btn-danger" onclick="UITest2.closeModal()" style="margin-top: 20px;">بستن</button>
                    `;
                    this.log('🪟 مودال خطا باز شد', 'error');
                } else if (type === 'srs') {
                    html = `
                        <h3 style="color: #e67e22; margin-bottom: 15px;">🧠 نتیجه مرور</h3>
                        <p>کیفیت پاسخ: ۴ (خوب)</p>
                        <p>فاصله مرور بعدی: ۶ روز</p>
                        <p>تاریخ مرور: ${new Date(Date.now() + 6*86400000).toLocaleDateString('fa-IR')}</p>
                        <button class="btn btn-orange" onclick="UITest2.closeModal()" style="margin-top: 20px;">تأیید</button>
                    `;
                    this.log('🪟 مودال SRS باز شد', 'info');
                }
                
                content.innerHTML = html;
                modal.style.display = 'flex';
            },
            
            closeModal() {
                document.getElementById('modal').style.display = 'none';
                this.log('🪟 مودال بسته شد', 'info');
            },
            
            // فرم
            validateForm() {
                const username = document.getElementById('username').value;
                const email = document.getElementById('email').value;
                const password = document.getElementById('password').value;
                
                let errors = [];
                if (username.length < 3) errors.push('نام کاربری باید حداقل ۳ کاراکتر باشد');
                if (!email.includes('@')) errors.push('ایمیل نامعتبر است');
                if (password.length < 6) errors.push('رمز عبور باید حداقل ۶ کاراکتر باشد');
                
                if (errors.length === 0) {
                    this.log('✅ فرم ثبت‌نام معتبر است', 'success');
                    this.openModal('success');
                } else {
                    errors.forEach(err => this.log(`❌ ${err}`, 'error'));
                }
            },
            
            // SRS
            logSRS(quality) {
                let qualityText = '';
                if (quality === 5) qualityText = 'عالی';
                else if (quality === 3) qualityText = 'خوب';
                else qualityText = 'دوباره';
                
                this.log(`🧠 ثبت کیفیت پاسخ: ${quality} (${qualityText})`, 'success');
                
                // محاسبه فاصله مرور
                let interval = 1;
                if (quality >= 4) interval = 6;
                if (quality >= 5) interval = 15;
                
                this.log(`📅 مرور بعدی: ${interval} روز دیگر`, 'info');
            },
            
            // فلش کارت
            flipCard() {
                const card = document.getElementById('flashcard');
                card.classList.toggle('flipped');
                this.log('🃏 فلش کارت برگردانده شد', 'info');
            },
            
            flipResult(isCorrect) {
                if (isCorrect) {
                    this.log('✅ کاربر کلمه را بلد است', 'success');
                } else {
                    this.log('🔄 کاربر کلمه را بلد نیست - نیاز به مرور بیشتر', 'warning');
                }
                this.flipCard();
            },
            
            // تایمر
            timerInterval: null,
            timeLeft: 300,
            
            startTimer() {
                if (this.timerInterval) clearInterval(this.timerInterval);
                this.timerInterval = setInterval(() => {
                    if (this.timeLeft > 0) {
                        this.timeLeft--;
                        const minutes = Math.floor(this.timeLeft / 60);
                        const seconds = this.timeLeft % 60;
                        document.getElementById('timer').textContent = 
                            `${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;
                    } else {
                        clearInterval(this.timerInterval);
                        this.log('⏱️ زمان مطالعه به پایان رسید', 'warning');
                    }
                }, 1000);
                this.log('⏱️ تایمر مطالعه شروع شد', 'success');
            },
            
            pauseTimer() {
                clearInterval(this.timerInterval);
                this.log('⏱️ تایمر متوقف شد', 'warning');
            },
            
            resetTimer() {
                this.timeLeft = 300;
                document.getElementById('timer').textContent = '۰۵:۰۰';
                clearInterval(this.timerInterval);
                this.log('⏱️ تایمر ریست شد', 'info');
            }
        };
        
        // بستن مودال با کلیک بیرون
        window.onclick = function(event) {
            const modal = document.getElementById('modal');
            if (event.target === modal) {
                modal.style.display = 'none';
                UITest2.log('🪟 مودال با کلیک بیرون بسته شد', 'info');
            }
        };
        
        window.onload = () => {
            UITest2.log('✅ تست کامپوننت‌های پیشرفته آماده است');
        };
    </script>
</body>
</html>
